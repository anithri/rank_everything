<% content_for :title, "Editing ranked list" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing ranked list</h1>

  <%= render "form", ranked_list: @ranked_list %>

  <%= link_to "Show this ranked list", @ranked_list, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
  <%= link_to "Back to ranked lists", team_ranked_lists_path(@team), class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
